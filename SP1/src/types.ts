// Власна модель даних застосунку. P1 потрібен один тип: місто таким, яким його бачить
// екран. У наступних практичних він росте крок за кроком — не переписуйте його,
// а розширюйте.

export type City = {
  /** Стабільна ідентичність: ним користуються ключ списку, id у базі і параметр маршруту. */
  id: string
  name: string
  country: string
  /** Поточна температура, °C — одна канонічна одиниця; форматування робить екран. */
  temperature: number
  /** Погода словами. API надішле число — перетворення числа на ці слова теж попереду. */
  condition: string
}
